{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    <div id="inscription" class="card col s8 offset-s2">
    <h1>Classe creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('classe_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
</div>
{% endblock %}
